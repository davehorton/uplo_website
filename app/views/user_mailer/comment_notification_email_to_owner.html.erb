<table>
  <tr>
    <td><%= @comment.description %></td>
    <td><%= link_to (image_tag @comment.image.url(:smallest), :alt => formatted_image_name(@comment.image)), browse_image_url(@comment.image) %></td>    
  </tr>
</table>
