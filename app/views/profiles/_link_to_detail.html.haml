/ local values: user_id, url
- if is_current_user(user_id)
  .edit-pane.right{ :data => { 'url' => "#{ url }"} }
    .icon.edit.left
    .text.black.font12.left Edit
- else
  .actions-panel.right
    %a.list.text.black.bold.font10.right{ :data => { 'url' => "#{ url }"} } + VIEW ALL
